class Solution {
public:
    int search(vector<int>& nums, int target) {
        int s=nums.size(),num=-1;
        for(int i=0;i<s;i++)
        {
            if(nums[i]==target)
                num=i;
        }
        return num;
    }
};
