class Solution {
public:
    bool isPalindrome(int x) {
       long int y=0;
        int dup=x;
        while(x>0)
        {
            y=y*10+x%10;
            x=x/10;
        }
        if(y==dup)
            return true;
        else
            return false;
    }
};
